{
  "name": "per-dashboard",
  "version": "1.0.0",
  "description": "Per's personal dashboard — calendar, inbox, Asana, Buddy Analytics KPIs. Password-gated, live fetch on every refresh.",
  "type": "module",
  "main": "server.js",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.0"
  }
}
